{
  "name": "Battleship",
  "devDependencies": {
    "@usebruno/cli": "^2.15.1"
  },
  "scripts": {
    "docker:build": "docker stop Battleship-container || true && docker rm Battleship-container || true && docker build -t Battleship . --progress=plain",
    "docker:run": "docker stop Battleship-container || true && docker rm Battleship-container || true && docker run -d -p 8080:8080 --name Battleship-container Battleship",
    "docker:stop": "docker stop Battleship-container",
    "docker:exec": "docker exec -it Battleship-container /bin/sh",
    "test:smoke": "curl -s http://localhost:8080/health | jq .",
    "test:integration": "cd api/collection/Battleship && bru run"
  }
}
